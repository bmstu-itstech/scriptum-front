services:
  web:
    build: 
      context: .
      args:
        NEXT_PUBLIC_API_URL: $NEXT_PUBLIC_API_URL
    ports:
      - "${EXTERNAL_PORT:-3000}:3000"
    environment:
      PORT: 3000
    restart: unless-stopped
